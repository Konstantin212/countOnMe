ENV=local

# For API (FastAPI)
API_HOST=0.0.0.0
API_PORT=8000

# Database
DATABASE_URL=postgresql+asyncpg://countonme:countonme@db:5432/countonme

# Auth
# Used to derive/verify device tokens (keep secret in real env)
DEVICE_TOKEN_PEPPER=change-me

